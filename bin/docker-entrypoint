#!/bin/bash

set -e

cp -r /usr/src/app/public /usr/src

if [ "${*}" == "./usr/src/app/bin/rails server" ]; then
	rm -f /usr/src/app/tmp/pids/server.pid
	/usr/src/app/bin/rails db:prepare
fi

exec "$@"
